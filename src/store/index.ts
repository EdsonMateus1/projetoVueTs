import Vue from "vue";
import Vuex from "vuex";
import { UserData } from "@/settings/types/userTypes";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    todos: [],
  },
  mutations: {
    
  },
  actions: {},
  modules: {},
});
